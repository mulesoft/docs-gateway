= MCP Personally Identifiable Information (PII) Detector Policy
ifndef::env-site,env-github[]
include::_attributes.adoc[]
endif::[]
:imagesdir: ../assets/images
:keywords: api gateway, flex gateway, gateway, policy

[width="100%", cols="5,15"]
|===
>s| Policy Name | MCP PII Detector Policy
>s|Summary      | Detects, mask, and redacts personally identifiable information (PII) in MCP server requests and responses
>s|Category | MCP
>s|First Flex Gateway version available | v1.9.3
>s| Returned Status Codes | Policy violation is reported when PII is detected
|===

== Summary

The MCP PII Detector policy detects, mask, and redacts personally identifiable information (PII) in MCP server requests and responses. When PII is detected in a JSON-RPC response's `result` field, the policy modifies the response to decline the action and reports a policy violation.

== Configuring Policy Parameters

include::partial$policy-title-headers.adoc[tag=configFileTitleOnly]

The MCP PII Detector policy isn't supported in Local Mode.

include::partial$policy-title-headers.adoc[tag=ui]

[%header%autowidth.spread,cols="a,a"]

|===
| Element | Description
|  Entities| Array that defines the types of PII to detect. You can select multiple PII types. Supported values are `Email`, `US SSN`, `Credit Card`, `Phone Number`.
 + 
For more information, see <<pii-types>>.
|===

[[pii-types]]
=== PII Types

When you configure an MCP PII Detector policy, you can choose which types of PII to detect.


The following table describes the type of PII this policy can detect:

[%header,cols='1a,5a'] 
|===
|PII Type
|Description

|Email
|Standard email addresses such as `User.Name+tag@example.com`

|US SSN
|United States Social Security Numbers (SSNs) in the standard format: `XXX-XX-XXXX` where each `X` is a digit. For example: `123-45-6789`.

|Credit Card
|Credit card numbers in the form of four groups of four digits, separated by optional spaces or hyphens. Examples: `1234-5678-9012-3456`, `1234 5678 9012 3456`, or `1234567890123456`.

|Phone Number
|United States phone numbers in various formats, with or without country code, parentheses, spaces, hyphens, or dots. Examples: `123-456-7890`, `(123) 456-7890`, `123.456.7890`, `+1 123 456 7890`, or `1234567890`.
|===

== How It Works

The MCP PII Detector policy intercepts POST requests with JSON content type and scans the JSON-RPC response payload for PII. When the policy detects PII in the `result` field of a JSON-RPC response, it:

* Reports a policy violation
* Modifies the response to reject the action with the reason: `PII data requested or found in user response`

The policy only scans JSON-RPC responses that contain a `result` field, which represents user responses to MCP server elicitation prompts. JSON-RPC requests (which contain a `method` field) are not scanned and continue normally.

