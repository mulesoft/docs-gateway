= Configuring Inbound TLS for Managed Flex Gateway
ifndef::env-site,env-github[]
include::_attributes.adoc[]
endif::[]
:imagesdir: ../assets/images
:page-mode: conn


For Managed Flex Gateway deployed on CloudHub 2.0, the CloudHub 2.0 private space manages the inbound TLS. 

To configure a TLS context the private space you're deploying your managed Gateway to, see xref:cloudhub-2::ps-config-domains.adoc[].

Configuring a TLS context for your private space when a Managed Gateway is deployed means that all API public endpoints are HTTPS and secure.

To configure outbound TLS context, see xref:flex-managed-outbound-tls-config.adoc[].