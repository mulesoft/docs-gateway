= GraphQL Introspection Control Policy
ifndef::env-site,env-github[]
include::_attributes.adoc[]
endif::[]
:imagesdir: ../assets/images
:keywords: graphql, introspection, api gateway, flex gateway, gateway, policy

[width="100%", cols="5,15"]
|===
>s| Policy Name | GraphQL Introspection Control
>s| Summary | Lets you block GraphQL introspection meta-fields `__schema`, `__type`, and `__typename` on incoming requests
>s| Category | Security
>s| First Flex Gateway version available | v1.13.0
.1+>.^s| Returned status codes
| `403` - The policy rejects the request and includes a header containg the restricted meta fields.
|===

== Summary

The GraphQL Introspection Control policy inspects each incoming GraphQL operation and blocks requests that reference configured GraphQL meta fields. You can allow or block these meta fields:

* `__schema`: used for full schema discovery
* `__type`: used to look up a type by name
* `__typename`: used to read the type name of an object

By default, this policy doesn't block any request.

After the policy resolves the GraphQL query, the policy evaluates the operation text. For persisted queries, the gateway resolves the document without the inline query text, and the requests pass through without introspection checks.

If a request includes a bloack meta field, the policy returns `403` error code with a `Content-Type: application/json` header that includes an  `errors` containing the restricted meta fields the request tried to access. For example, `{"errors":[{"message":"Introspection field '__schema' is not allowed"}]}`.


== Configuring Policy Parameters

include::partial$policy-title-headers.adoc[tag=configFile]

----
- policyRef:
    name: graphql-introspection-control-flex
  config:
    blockSchema: <boolean> // OPTIONAL, default: false
    blockType: <boolean> // OPTIONAL, default: false
    blockTypename: <boolean> // OPTIONAL, default: false
----

[%header%autowidth.spread,cols="a,a,a,a"]
|===

| Parameter | Required | Default Value | Description

| `blockSchema`
| Optional
| `false`
| When `true`, the policy blocks requests that reference the `__schema` meta field.

| `blockType`
| Optional
| `false`
| When `true`, the policy returns 403 when the operation references the `__type` meta-field.

| `blockTypename`
| Optional
| `false`
| When `true`, the policy returns 403 when the operation references the `__typename` meta-field.

|===

==== Resource Configuration Example

The following example blocks schema and type introspection while still letting clients use `__typename` where they need it:

[source,yaml]
----
- policyRef:
    name: graphql-introspection-control-flex
  config:
    blockSchema: true
    blockType: true
    blockTypename: false
----

include::partial$policy-title-headers.adoc[tag=ui]

[%header%autowidth.spread,cols="a,a,a,a"]
|===
s|Field s|Description s|Default Value s|Required or Optional

| Block `__schema`
| When enabled, clients can't use the `__schema` meta-field to discover the full API schema.
| Disabled, schema introspection allowed | Optional

| Block `__type`
| When enabled, clients can't use the `__type` meta-field to query details about a specific type by name.
| Disabled, type introspection allowed | Optional

| Block `__typename`
| When enabled, clients can't use the `__typename` meta-field to read the concrete type name of an object.
| Disabled, `__typename` allowed | Optional

| Advanced options
| Standard policy options such as policy version and application rules.
| |
|===

[NOTE]
====
In Anypoint Platform, *Block* toggles map directly to the declarative flags. When you enable *Block `__schema`*, Anypoint Platform sets `blockSchema` to `true` in the policy configuration that Flex Gateway receives.
====

== See Also

* xref:policies-included-directory.adoc[Inbound Policies Directory]
* xref:flex-gateway-secure-apis.adoc[Securing Flex Gateway Instances with Policies]
